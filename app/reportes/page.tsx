type Grupo = [string, string[]];

const grupos: Grupo[] = [["Cartera",["Cartera activa","Posiciones disponibles","Inmuebles por etapa","Inmuebles por estado"]],["Gestión",["Visitas realizadas","Tasaciones realizadas","Pendientes","Listos para publicar"]],["Salidas",["Vendidos","Retirados / cancelados","Motivos de liberación"]],["Histórico",["Histórico de inmuebles","Tiempo de permanencia","Posición ocupada"]],["Flujo",["Registro → visita","Visita → tasación","Tasación → aprobación","Aprobación → publicación"]]];

export default function Page(){return <main className="min-h-screen bg-slate-50 p-6 lg:p-8"><p className="text-xs font-bold uppercase tracking-[.14em] text-slate-400">Fase 1</p><h1 className="mt-1 text-2xl font-bold text-slate-950">Reportes</h1><p className="mt-2 text-sm text-slate-500">Información para consultar la cartera, la gestión y el histórico.</p><div className="mt-6 grid gap-4 md:grid-cols-2 xl:grid-cols-3">{grupos.map(([nombre, reportes])=><section key={nombre} className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><h2 className="font-bold text-slate-900">{nombre}</h2><div className="mt-3 space-y-2">{reportes.map((reporte)=><button key={reporte} className="flex w-full items-center justify-between rounded-xl border border-slate-100 px-3 py-2.5 text-left text-sm text-slate-700 hover:bg-slate-50">{reporte}<span>→</span></button>)}</div></section>)}</div></main>}